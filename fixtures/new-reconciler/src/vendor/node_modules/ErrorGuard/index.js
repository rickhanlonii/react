/**
 * Copyright 2004-present Facebook. All Rights Reserved.
 *
 * @emails oncall+web_speed
 * @flow strict
 * @format
 */

'use strict';

const ErrorGuard = {
  applyWithGuard: function(func, context, args) {
    return func.apply(context, args);
  },
  guard: function(func) {
    return func;
  },
};

export default ErrorGuard;
