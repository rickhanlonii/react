export default function Page() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100vh',
        color: '#8e8e93',
        fontSize: 16,
      }}>
      Select a fixture from the sidebar
    </div>
  );
}
