// Placeholder JS bundle — replaced by scripts/build-js.sh
// This file exists so the Xcode project compiles without a build step.
(function() {
  'use strict';
  if (typeof $$log !== 'undefined') {
    $$log('react-dom-native bundle loaded (placeholder)');
  }
})();
